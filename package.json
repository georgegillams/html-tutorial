{
  "name": "html-tutorial",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "start": "nodemon --watch webpage -e html --exec \"http-server ./webpage & open http://localhost:8080\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "http-server": "^0.12.3",
    "nodemon": "^2.0.4"
  }
}
